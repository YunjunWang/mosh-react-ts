/**
 * Use index.ts file to export everything from
 * the current folder so
 * anywhere needs to use it don't have to 
 * import with long path
 */
import NativeCss from "./NativeCss";

export default NativeCss;
